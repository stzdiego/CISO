@page "/Roles"
@attribute [STZAuthorize("Roles")]
@using CISO.ManagementService.Shared.Entities
@using Microsoft.AspNetCore.Components.Web
@using STZ.Frontend.Components
@using STZ.Frontend.Services
@using MudBlazor
@using STZ.Frontend.Authorization
@inject LocalizationService Localization

<PageTitle><STZText Key="Roles.Title" /></PageTitle>

<STZDataGrid TItem="Role" TId="Guid" Title="Roles.Title" DetailComponentType="typeof(View)" EditComponentType="typeof(Edit)" AddComponentType="typeof(Add)" >
    <Columns>
        <STZColumn T="Role" TProperty="string" Property="x => x.Name" Title="General.Name" Hideable="true"/>
        <STZColumn T="Role" TProperty="DateTime" Property="x => x.CreatedAt" Title="General.CreationDate" Hideable="true"/>
        <STZColumn T="Role" TProperty="DateTime?" Property="x => x.UpdatedAt" Title="General.UpdateDate" Hidden="true" Hideable="true"/>
    </Columns>
</STZDataGrid>