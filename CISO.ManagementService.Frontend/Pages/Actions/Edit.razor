@using MudBlazor
@using Action = CISO.ManagementService.Shared.Entities.Action
@using STZ.Frontend.Components
@using STZ.Frontend.Services
@inject LocalizationService Localization

<MudForm>
    <MudGrid>
        <MudItem xs="12" sm="6">
            <MudTextField T="string" Label="@Localization.Get("General.Name")" @bind-Value="Item.Name" />
        </MudItem>
    </MudGrid>
    
    <MudStack Row Class="mt-4" Justify="Justify.FlexEnd">
        <MudButton OnClick="() => MudDialog.Cancel()"><STZText Key="General.Cancel" /></MudButton>
        <MudButton Color="Color.Primary" Variant="Variant.Filled" OnClick="() => MudDialog.Close(DialogResult.Ok(Item))"><STZText Key="General.Update" /></MudButton>
    </MudStack>
</MudForm>

@code {
    [Parameter] public  required Action Item { get; set; }
    [CascadingParameter] private IMudDialogInstance MudDialog { get; set; } = default!;

}